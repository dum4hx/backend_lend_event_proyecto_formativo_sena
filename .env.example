PORT=8080

# DB
DB_CONNECTION_STRING=#mongodb connection string here

# JWT
JWT_ASYMMETRIC_KEY_ALG='RS256'
JWT_ENC='A256GCM'
JWT_ISSUER='https://api.test.local/'
JWT_AUDIENCE='https://app.test.local/'

COOKIE_DOMAIN=test.local

# --- INITIAL SETUP ---#
# Super admin user for one-time seeder (npm run seed:admin)
# These credentials will be used to create the initial platform administrator
INITIAL_ADMIN_EMAIL=admin@example.com
INITIAL_ADMIN_PASSWORD=ChangeMe123!

# --- BILLING ---#
# Stripe API keys
STRIPE_SECRET_KEY=#your stripe secret key here
STRIPE_WEBHOOK_SECRET=#your stripe webhook secret here

# --- EMAIL (SMTP) ---#
# SMTP configuration for transactional emails (password reset, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=#your SMTP username/email here
SMTP_PASS=#your SMTP password or app-specific password here
SMTP_FROM=noreply@lendevent.com

# --- TESTING ---#
# Super admin credentials for E2E tests
SUPER_ADMIN_EMAIL=superadmin@test.local
SUPER_ADMIN_PASSWORD=SuperAdmin123!

# Environment settings
NODE_ENV=development